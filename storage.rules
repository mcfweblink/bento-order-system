rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 商品画像は認証済みの管理者のみが書き込める
    match /products/{allPaths=**} {
      allow read: if true; // 誰でも読み取り可能
      allow write: if request.auth != null; // 認証済みユーザーのみ書き込み可能
    }
  }
}